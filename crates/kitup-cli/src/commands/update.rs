pub fn run(
    _tools: Vec<String>,
    _all: bool,
    _install: bool,
    _dry_run: bool,
    _force: bool,
    _parallel: usize,
) -> anyhow::Result<()> {
    println!("update command - not yet implemented");
    Ok(())
}
